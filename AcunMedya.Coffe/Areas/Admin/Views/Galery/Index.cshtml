@model List<AcunMedya.Coffe.Entities.Galery>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<div class="content-wrapper">
    <div class="page-header">
        <h3 class="page-title"> Galeri Tablsou </h3>
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="#">Tablo</a></li>
                <li class="breadcrumb-item active" aria-current="page">Galeri</li>
            </ol>
        </nav>
    </div>
    <div class="row">
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>Resim</th>
                                <th>Güncelle</th>
                                <th>Sil</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.GaleryId</td>
                                    <td>
                                        <img src="@item.ImageUrl" alt="Resim" style="width: 100px; height: 100px;" />
                                    <td><a href="/Admin/Galery/UpdateGalery/@item.GaleryId" class="badge badge-info">Güncelle</a></td>
                                    <td><a href="/Admin/Galery/DeleteGalery/@item.GaleryId" class="badge badge-danger">Sil</a></td>
                                </tr>
                            }


                        </tbody>
                    </table>
                    <a href="/Admin/Galery/AddGalery" class="btn btn-primary"><i class="fa fa-plus-circle"></i> Yeni Resim</a>
                </div>
            </div>
        </div>

    </div>
</div>